// Array of friend names
let names = ["Ali", "Babar", "Dawood", "Danish", "Zubair"];

// Print each person's name one at a time
console.log("My friends' names:");

for (let i = 0; i < names.length; i++) {
  console.log(names[i]);
}
